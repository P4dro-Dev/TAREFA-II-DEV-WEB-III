    # Relatório — Tarefa II: StudyTasks


    **Aluno:** Pedro Henrique


    ## Link do repositório

Coloque o link do repositório GitHub aqui após o envio.


    ## Passos realizados

1. Criação do projeto Django e app `sistema`.
2. Implementação do padrão Front Controller via `studytasks_project/urls.py`.
3. Implementação do padrão MVT com modelo `Tarefa`, views, templates.
4. Criação de rotas para index, problema, solucao e autor.
5. Adição de arquivo README com instruções para execução.

## Prints / Demonstrações

Inclua screenshots do sistema rodando localmente (ex.: http://127.0.0.1:8000/) e do histórico de commits mostrando branch `organizacao` e merge.

## Observações

- O arquivo `db.sqlite3` não está incluído por padrão; rode `makemigrations` e `migrate` localmente se desejar um banco limpo.
